import styled from "styled-components";

const WeatherCard = () => {
  return <WeatherCardContainer> this is weather card </WeatherCardContainer>;
};

const WeatherCardContainer = styled.div`
  width: 100%;
`;
export default WeatherCard;
